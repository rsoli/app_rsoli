import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kolosu.rastreo',
  appName: 'Kolosu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
